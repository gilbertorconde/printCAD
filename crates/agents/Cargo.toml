[package]
name = "agents"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "AI agents: the Agent Client Protocol client and the MCP server the application offers them."

[dependencies]
base64.workspace = true
serde_json.workspace = true
