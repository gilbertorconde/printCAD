[package]
name = "gear"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
printcad-bench-sdk = { path = "../../printcad-bench-sdk" }
serde = { version = "1", features = ["derive"] }
